package com.Example4;

import java.util.Arrays;

public class Exam44 {
    public static void main(String[] args) {
        // int[] numbers = {3, 4, 9};
        // System.out.println("1자리의 숫자를 입력해 주세요");
        // int low = 0;
        // int high = numbers.length - 1;
        // int mid;
        // int input = new java.util.Scanner(System.in).nextInt();
        //
        // while (low <= high) {
        // mid = (low + high) / 2;
        // if (input == numbers[mid]) {
        // System.out.println("정답!");
        // return;
        // } else if (input < numbers[mid]) {
        // high = mid - 1;
        // } else {
        // low = mid + 1;
        // }
        // }

        // int[] n = {1, 2, 3};
        // n = null;
        // n = new int[] {2,3,4};

        /*
         * for (int i = 0; i < numbers.length; i++) { if (numbers[i] == input) {
         * System.out.println("입력값과 일치하는 값이 있습니다: " + input); break; } }
         */
    }
}
